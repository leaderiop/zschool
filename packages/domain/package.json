{
  "name": "@zschool/domain",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "oxlint .",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "effect": "4.0.0-rc.113",
    "@zschool/db": "workspace:*",
    "@qadi/core": "0.5.0"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.113"
  }
}
