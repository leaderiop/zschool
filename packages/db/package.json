{
  "name": "@zschool/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "oxlint .",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:rls": "dotenv -e ../../.env -c -- vitest run --config vitest.rls.config.ts",
    "migrate": "dotenv -e ../../.env -c -- tsx src/runMigrations.ts"
  },
  "dependencies": {
    "effect": "4.0.0-rc.113",
    "@effect/sql-pg": "4.0.0-rc.113",
    "@effect/platform-node": "4.0.0-rc.113"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.113",
    "tsx": "^4.20.6"
  }
}
