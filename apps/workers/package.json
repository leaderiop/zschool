{
  "name": "@zschool/workers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "oxlint .",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@zschool/domain": "workspace:*",
    "@zschool/db": "workspace:*",
    "@zschool/infra": "workspace:*"
  }
}
